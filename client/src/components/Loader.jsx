// Kept as a compatibility alias for the real, actively-used loader at
// components/common/Loader.jsx, so imports of either path work and the
// spinner styling only needs to be maintained in one place.
export { default } from "./common/Loader";
